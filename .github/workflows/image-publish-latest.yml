name: Image Publish Latest (GHCR)
on:
  workflow_run:
    workflows: [CI]
    types: [completed]
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
  packages: write
concurrency:
  group: image-publish-latest-ghcr
  cancel-in-progress: false
env:
  GHCR_IMAGE: ghcr.io/nanashithenameless/namelesspronouns
jobs:
  publish:
    name: Build and Push Latest
    runs-on: ubuntu-latest
    timeout-minutes: 30
    if: >-
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'workflow_run' &&
      github.event.workflow_run.event == 'push' &&
      github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.head_branch == 'main')
    steps:
      - name: Checkout trusted main
        uses: actions/checkout@v7
        with:
          ref: refs/heads/main
          persist-credentials: false
      - name: Get build metadata
        id: build-meta
        shell: bash
        run: |
          echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"
          echo "time=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> "$GITHUB_OUTPUT"
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4
      - name: Set up Buildx
        uses: docker/setup-buildx-action@v4
      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}
      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.GHCR_IMAGE }}
          flavor: latest=true
          tags: type=raw,value=latest
          labels: |
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
            org.opencontainers.image.revision=${{ steps.build-meta.outputs.sha }}
            org.opencontainers.image.created=${{ steps.build-meta.outputs.time }}
            org.opencontainers.image.description=NamelessPronouns container image
      - name: Build and push image
        uses: docker/build-push-action@v7
        with:
          context: .
          target: runtime
          push: true
          platforms: linux/amd64,linux/arm64
          provenance: mode=min
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - name: Delete old untagged GHCR versions
        uses: NanashiTheNameless/delete-package-versions@main
        with:
          package-name: namelesspronouns
          package-type: container
          min-versions-to-keep: 4
          delete-only-untagged-versions: true
